/#nugget
%h1
  = image_tag @artist.image.url(:medium)
  = @artist.name 
%br
%br
= @artist.bio       
%br
%br
%br
(
= link_to 'Homepage', "#{@artist.external_links}"
)
%br
%br
%br
%hr         
%h3 Albums
%hr
%br
- @artist.albums.each do |album|
  %a{:href => "/albums/#{album.id}"}
    = image_tag album.image.url(:small)
    = album.title
    (
    = album.release_year
    )
    %p
      %hr
  %br

= link_to 'Back to Artists', url(:artists)  