.row 
= file_field :track, :label => '<b>Upload MP3</b>'
.row
= text_field :title, :label => '<b>Song Title</b>'
.row
= select :artist_id,:label=>"<b>Artist:</b>", :collection => Artist.all.map {|a| [a.id, a.name]}
.row 
= select :album_id, :label => '<b>Which Album?</b>', :collection => Album.all.map { |a| [a.id, a.title] }  

