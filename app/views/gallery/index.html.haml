= js_include_tag :lightbox
= css_include_tag :lightbox

#gallery  
  - @arts.each do |art|
    %a{:href => "#{art.image.url(:large)}", :title => "#{art.description}", :rel => "gallery", :alt => "#{art.title}"}
      = image_tag art.image.url(:medium)
    