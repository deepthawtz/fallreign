.error
  = error_messages_for @link

= form_for(@link, :action => url(:links)) do
  #title Edit Link
  .row
  = text_field :title, :label => "Link Name: "
  .row
  = text_field :url, :label => "Link Address: "
  .submit
  = submit "Create"

= link_to 'Back', url(:links)