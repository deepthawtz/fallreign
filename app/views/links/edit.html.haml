= error_messages_for @link

= form_for(@link, :action => url(:link, @link)) do
  #title Edit Link
  .row
  = text_field :title, :label => "Link Name: "
  .row
  = text_field :url, :label => "Link Address: "
  .submit
  = submit "Update"

= link_to 'Show', url(:link, @link)
|
= link_to 'Back to admin tab', "/admin#ui-tabs-15"
|
= link_to 'Back', url(:links)